para usar el programa hace falta tener xampp instalado(iniciar apache y mysql),
despues con un editor de texto(yo use visual studio code),
hacer npm install y npm start,
despues de eso irse a la direccion http://localhost:4000/principal para probar.
